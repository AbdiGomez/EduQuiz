<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.text.*?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.example.eduquiz.controllers.FinalController"
           style="-fx-background-color: linear-gradient(to bottom, #89f7fe, #66a6ff);"
           prefWidth="720" prefHeight="520">

    <VBox alignment="CENTER" spacing="20">

        <Label fx:id="titleLabel"
               text="¡Nivel completado!"
               style="-fx-font-size: 34px; -fx-font-weight: bold; -fx-text-fill: white;"/>

        <!-- Contenedor de estrellas -->
        <HBox fx:id="starsBox" alignment="CENTER" spacing="15"/>

        <!-- Estadísticas -->
        <VBox alignment="CENTER" spacing="8">

            <Label fx:id="correctLabel"
                   style="-fx-font-size: 20px; -fx-text-fill: white;"/>

            <Label fx:id="wrongLabel"
                   style="-fx-font-size: 20px; -fx-text-fill: white;"/>

            <Label fx:id="percentageLabel"
                   style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #ffee58;"/>

            <Label fx:id="attemptsLabel"
                   style="-fx-font-size: 18px; -fx-text-fill: white;"/>

        </VBox>

        <!-- Botones -->
        <HBox spacing="30" alignment="CENTER">

            <Button fx:id="nextLevelButton"
                    text="⬆ Avanzar al siguiente"
                    onAction="#onNextLevel"
                    style="-fx-background-color: #00c853; -fx-text-fill:white; -fx-font-size: 18px;
                           -fx-padding: 10 25; -fx-background-radius: 20;"/>

            <Button fx:id="retryButton"
                    text="🔄 Repetir nivel"
                    onAction="#onRetry"
                    style="-fx-background-color: #ff7043; -fx-text-fill:white; -fx-font-size: 18px;
                           -fx-padding: 10 25; -fx-background-radius: 20;"/>

            <Button fx:id="exitButton"
                    text="🚪 Salir"
                    onAction="#onExit"
                    style="-fx-background-color: #d50000; -fx-text-fill:white; -fx-font-size: 18px;
                           -fx-padding: 10 25; -fx-background-radius: 20;"/>

        </HBox>

    </VBox>

</StackPane>
